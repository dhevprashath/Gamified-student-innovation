/* Feature modules.
 *
 * Each module owns its complete vertical slice:
 *   features/<module>/
 *     api/        - endpoint functions for this module
 *     components/ - module-specific React components
 *     hooks/      - TanStack Query hooks
 *     pages/      - routed page components
 *
 * Phase 1 ships only `home` (a connectivity check). New modules are added per phase:
 * auth, dashboard, ideas, team, agents, gamification. */
